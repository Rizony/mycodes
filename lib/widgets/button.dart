import 'package:flutter/material.dart';

class MyWidget extends StatelessWidget {
  var buttontext;
  final VoidCallback callback;

  MyWidget({required this.buttontext, required this.callback});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
